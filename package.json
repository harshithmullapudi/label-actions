{
  "name": "label-actions",
  "version": "0.1.0",
  "description": "A GitHub Action that performs actions when issues or pull requests are created",
  "author": "Harshith Mullapudi",
  "license": "MIT", 
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "update": "ncu --upgrade",
    "release": "standard-version",
    "push": "git push --tags origin master"
  },
  "dependencies": {
    "@actions/core": "^1.4.0",
    "@actions/github": "^5.0.0",
    "joi": "^17.4.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.28.6",
    "npm-check-updates": "^11.8.1",
    "prettier": "^2.3.2",
    "standard-version": "^9.3.0"
  },
  "engines": {
    "node": ">=12.0.0"
  },

  "private": true
}
